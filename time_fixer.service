[Unit]
Description=Fix system clock via HTTP then NTP (for unreliable CMOS clocks)
Wants=network-online.target
After=network-online.target

[Service]
Type=oneshot
ExecStart=/opt/time_fixer/time_fixer.sh
